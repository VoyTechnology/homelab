fullnameOverride: seaweedfs

master:
  replicas: 1
  annotations:
    argocd.argoproj.io/sync-wave: "0"

volume:
  replicas: 1
  annotations:
    argocd.argoproj.io/sync-wave: "0"

filer:
  replicas: 1
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  s3:
    enabled: true

s3:
  enabled: true
  replicas: 1
  enableAuth: false
  annotations:
    argocd.argoproj.io/sync-wave: "0"

seaweedfs:
  createBuckets:
    - name: mimir
