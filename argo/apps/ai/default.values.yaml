# Ollama config
ollama:
  models:
    pull:
    - gemma3:4b # Smaller model due to memory constraints
    run:
    - gemma3:4b # Preload

# open-webui config
ollamaUrls:
- ollama.ai.svc.

persistence:
  size: 10Gi
